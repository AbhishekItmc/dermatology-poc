[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
markers =
    property: Property-based tests (run with -m property)
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests
